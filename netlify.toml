[build]
  command = "npm ci && npx ng build --configuration production"
  publish = "dist/ohm-map/browser"

[build.environment]
  NODE_VERSION = "22"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
